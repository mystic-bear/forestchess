# Forest Chess Bugfix Progress

**Timestamp:** 2026-03-12 14:54:01
**Scope:** Phase 3 browser bug patch, start-screen UI fix, piece emoji update

## Summary

- Fixed the start-screen custom setup panel so it stays hidden until the user presses `Open custom setup`.
- Hardened browser worker URL resolution for both the top-level AI worker and the nested Stockfish worker.
- Reordered browser engine candidates to prefer `stockfish-18-lite-single.js`, which is the smallest checked-in browser engine that boots reliably in the current repo layout.
- Replaced board piece text with animal emoji and added hover titles such as `고양이 (폰)`.

## Code Changes

- `css/style.css`
  - Added a global `.hidden` rule.
  - Resized board pieces for emoji-first rendering and removed inline piece text styling.
- `js/app.js`
  - Forced the start menu into the collapsed setup state on boot.
- `js/ai-bridge.js`
  - Resolved the worker URL from `window.location.href` before spawning.
- `js/ui.js`
  - Render board pieces as emoji-only nodes.
  - Added square and piece hover labels using the full animal/chess name.
- `shared/constants.js`
  - Switched piece theme labels to Korean animal/chess names.
  - Reordered engine candidates to `lite-single -> asm -> lite`.
- `worker/stockfish-adapter.js`
  - Matched the new browser engine candidate order.
  - Resolved nested engine worker URLs from the worker location.

## Verification

- `node --check shared/constants.js`
- `node --check js/ui.js`
- `node --check js/app.js`
- `node --check js/ai-bridge.js`
- `node --check worker/stockfish-adapter.js`
- `node test/phase2-static-smoke.js`
- `node test/phase3-worker-bootstrap.js`
- `node test/phase3-stale-response.js`
- `node test/phase3-choosemove-smoke.js`
- `node test/phase3-hint-smoke.js`

## Result

- The custom setup panel no longer leaks onto the start screen.
- The active engine path from smoke testing is now `../stockfish-18-lite-single.js`.
- Board pieces are shown as animal emoji, and hovering a piece shows labels like `고양이 (폰)`.
