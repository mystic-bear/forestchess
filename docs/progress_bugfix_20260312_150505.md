# Forest Chess Bugfix Progress

**Timestamp:** 2026-03-12 15:05:05
**Scope:** file-origin worker diagnosis, board sizing stabilization

## Summary

- Checked the worker startup error shown in the board info panel.
- Confirmed the reported `origin 'null'` / `file:///...` worker failure is the direct-local-file launch case.
- Added a dedicated `file://` guard so the app shows a clear AI/coach limitation message instead of the raw browser exception.
- Stabilized the board size on desktop by capping the board header and board shell width to a centered maximum size.

## Code Changes

- `js/ai-bridge.js`
  - Detects `window.location.protocol === "file:"` before creating the worker.
  - Reports a clear message explaining that AI and coach require `http://localhost` or a deployed site.
- `css/style.css`
  - Added `--board-max-size: 760px`.
  - Centered the board header and board shell to that width cap.
  - Prevented the board block from stretching with the surrounding layout.

## Verification

- `node --check js/ai-bridge.js`
- `node test/phase2-static-smoke.js`
- `node test/phase3-worker-bootstrap.js`
- `node test/phase3-choosemove-smoke.js`
- `node test/phase3-hint-smoke.js`

## Result

- The worker error shown in the screenshot is now identified as the `file://` launch path.
- AI/coach still work under the normal worker pipeline.
- The board keeps a more consistent size on desktop instead of expanding and shrinking with spare layout width.
