# Forest Chess Bugfix Progress

**Timestamp:** 2026-03-12 15:16:53
**Scope:** desktop board column width stabilization

## Summary

- Rechecked the board sizing issue after the previous max-width patch.
- Confirmed the problem was not push/caching anymore because the updated code was already visible on multiple machines.
- Tightened the desktop game-grid layout so the center chess column no longer uses a flexible `1fr` track.

## Code Changes

- `css/style.css`
  - Added `--board-panel-width: 796px`.
  - Changed the desktop grid from a flexible middle column to a fixed-width board column.
  - Centered the three-column layout.
  - Gave `.board-panel` an explicit width cap and centered self-alignment.

## Verification

- `node test/phase2-static-smoke.js`

## Result

- The center board region should now remain visually stable on desktop instead of continuing to grow with the page width.
- This change is a layout fix only. It does not require deleting old commits or cleaning git history.
